import{exportJWK,generateKeyPair,SignJWT}from'jose';import{afterEach,beforeEach,describe,expect,it,vi}from'vitest';import{verifyAccess}from'../src/services/access-service';
let privateKey:CryptoKey;let publicJwk:Record<string,unknown>;const audience='app-audience';const team=`unit-${crypto.randomUUID()}.cloudflareaccess.com`;
beforeEach(async()=>{const pair=await generateKeyPair('RS256',{extractable:true});privateKey=pair.privateKey as CryptoKey;publicJwk={...await exportJWK(pair.publicKey),kid:'test-key',alg:'RS256',use:'sig'};vi.stubGlobal('fetch',vi.fn(async()=>Response.json({keys:[publicJwk]})));});afterEach(()=>vi.unstubAllGlobals());
async function token(overrides:Record<string,unknown>={}){let builder=new SignJWT({email:'admin@example.com',...overrides}).setProtectedHeader({alg:'RS256',kid:'test-key'}).setIssuer(`https://${team}`).setAudience(audience).setSubject('user-id').setIssuedAt();builder='exp'in overrides?builder:builder.setExpirationTime('5m');return builder.sign(privateKey);}
const request=async(value:string)=>new Request('https://admin.example.com',{headers:{'Cf-Access-Jwt-Assertion':value}});
describe('Cloudflare Access JWT',()=>{it('validates signature, issuer, audience, expiry and allowed email',async()=>expect(await verifyAccess(await request(await token()),team,audience,'other@example.com, ADMIN@example.com')).toEqual({email:'admin@example.com',subject:'user-id'}));it('returns 403 for missing or forged assertions',async()=>{await expect(verifyAccess(new Request('https://x'),team,audience,'admin@example.com')).rejects.toMatchObject({status:403});const other=await generateKeyPair('RS256');const forged=await new SignJWT({email:'admin@example.com'}).setProtectedHeader({alg:'RS256',kid:'test-key'}).setIssuer(`https://${team}`).setAudience(audience).setSubject('x').setExpirationTime('5m').sign(other.privateKey);await expect(verifyAccess(await request(forged),team,audience,'admin@example.com')).rejects.toMatchObject({status:403});});it.each([{aud:'wrong'},{email:'attacker@example.com'},{exp:Math.floor(Date.now()/1000)-60}])('rejects invalid claims %o',async(overrides)=>{let value;if('aud'in overrides){value=await new SignJWT({email:'admin@example.com'}).setProtectedHeader({alg:'RS256',kid:'test-key'}).setIssuer(`https://${team}`).setAudience(overrides.aud!).setSubject('x').setExpirationTime('5m').sign(privateKey);}else value=await token(overrides);await expect(verifyAccess(await request(value),team,audience,'admin@example.com')).rejects.toMatchObject({status:403});});});
