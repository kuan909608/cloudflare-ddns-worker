import {describe,expect,it} from'vitest';import{clientInputSchema,publicClient}from'../src/application/admin-clients';
const input={displayName:'Home',slug:'home-1',zoneId:'1'.repeat(32),zoneName:'example.com',recordId:'2'.repeat(32),recordName:'home.example.com',recordType:'A' as const};
describe('admin validation',()=>{it('accepts a fixed record binding',()=>expect(clientInputSchema.safeParse(input).success).toBe(true));it.each([{...input,slug:'../admin'},{...input,recordType:'TXT'},{...input,recordName:'x'},{...input,hostname:'attacker.example.com'}])('rejects injection or invalid fields',value=>expect(clientInputSchema.safeParse(value).success).toBe(false));it('never serializes token hash',()=>{const result=publicClient({id:crypto.randomUUID(),...input,enabled:true,tokenHash:'secret',tokenCreatedAt:'now',lastIp:null,lastSourceIp:null,lastStatus:null,lastUpdatedAt:null,createdAt:'now',updatedAt:'now'});expect(result).not.toHaveProperty('tokenHash');expect(result.tokenConfigured).toBe(true);});});
